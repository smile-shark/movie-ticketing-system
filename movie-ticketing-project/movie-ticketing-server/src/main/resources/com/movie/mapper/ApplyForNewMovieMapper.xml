<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.movie.mapper.ApplyForNewMovieMapper">
    <select id="applyForNewMovieTotal" resultType="int">
        select count(apply_id) from apply_for_new_movie
    </select>
</mapper>